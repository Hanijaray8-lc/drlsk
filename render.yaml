services:
  - type: web
    name: whatsapp-bot
    env: node
    region: oregon
    plan: free
    buildCommand: npm install
    startCommand: node whatsapp_bot_render.js
    envVars:
      - key: PUPPETEER_SKIP_DOWNLOAD
        value: "false"
      - key: NODE_ENV
        value: production
